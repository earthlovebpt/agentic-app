[project]
name = "agentic-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.44.1",
]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
]
